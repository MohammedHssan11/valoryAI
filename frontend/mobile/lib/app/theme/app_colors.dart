import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Background
  static const Color backgroundPrimary = Color(0xFF050B14);
  static const Color backgroundSecondary = Color(0xFF081423);

  // Surface
  static const Color surface = Color(0xFF0B1C2E);

  // Accent
  static const Color accent = Color(0xFF00D8FF);
  static const Color secondaryAccent = Color(0xFF2AE6B8);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFD9E1EA);
  static const Color textMuted = Color(0xFF94A3B8);
}
