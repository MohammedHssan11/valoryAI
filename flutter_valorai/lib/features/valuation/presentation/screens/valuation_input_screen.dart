import 'package:flutter/material.dart';

class ValuationInputScreen extends StatelessWidget {
  const ValuationInputScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ValuationInputScreen'),
      ),
    );
  }
}
