import 'package:flutter/material.dart';

class ComparableExplorerScreen extends StatelessWidget {
  const ComparableExplorerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ComparableExplorerScreen'),
      ),
    );
  }
}
